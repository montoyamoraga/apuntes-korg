name: compilar y subir PDFs

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: install LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y latexmk texlive-latex-extra texlive-lang-spanish texlive-fonts-recommended

      - name: compile English PDF
        run: |
          latexmk -pdf -interaction=nonstopmode -jobname=en en/en.tex

      - name: compile Spanish PDF
        run: |
          latexmk -pdf -interaction=nonstopmode -jobname=es es/es.tex

      - name: preparar sitio
        run: |
          mkdir site
          cp en.pdf site/en.pdf
          cp es.pdf site/es.pdf

          cat > site/index.html << 'EOF'
          <!DOCTYPE html>
          <html>
          <head>
            <meta charset="utf-8">
            <title>montoyamoraga - apuntes korg</title>
            <style>
              body { font-family: sans-serif; margin: 0; }
              .buttons { padding: 1rem; background: #111; }
              button { margin-right: 1rem; padding: 0.5rem 1rem; color: white; background: #333; border: none; cursor: pointer; }
              iframe { width: 100%; height: 90vh; border: none; }
            </style>
          </head>
          <body>
            <div class="buttons">
              <button onclick="loadPDF('en.pdf')">English</button>
              <button onclick="loadPDF('es.pdf')">Español</button>
            </div>
            <iframe id="viewer" src="en.pdf"></iframe>

            <script>
              function loadPDF(file) {
                document.getElementById('viewer').src = file;
              }
            </script>
          </body>
          </html>
          EOF

      - name: upload artifact for Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4